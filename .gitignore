node_modules/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Secrets & local DB
.env
*.db

# 생존편향 없는 벤더 데이터 (Norgate/Sharadar 등 — 라이선스상 재배포 금지 + 용량)
data/
*.parquet

# 시뮬 생성 산출물 (run_sim 성과·진단 리포트 — 재생성 가능, 커밋 금지)
reports/

# phase execution outputs
phases/**/phase*-output.json
phases/**/step*-output.json
